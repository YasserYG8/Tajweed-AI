numpy>=1.20.0
librosa>=0.10.0
soundfile>=0.12.0
pydantic>=2.0.0
# whisperx (Optional: needs CUDA and specialized installation, run manually)
# torch>=2.0.0 (Optional: required if running local neural networks)
# transformers>=4.30.0 (Optional: required for HuggingFace local Wav2Vec2 models)
